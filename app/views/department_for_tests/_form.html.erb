<%= form_with(model: department_for_test, local: true) do |form| %>
  <% if department_for_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(department_for_test.errors.count, "error") %> prohibited this department_for_test from being saved:</h2>

      <ul>
        <% department_for_test.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
